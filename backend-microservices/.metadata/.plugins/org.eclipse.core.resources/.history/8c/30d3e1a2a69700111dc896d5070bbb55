server:
  port: 8081

spring:
  application:
    name: user-auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/serviceit_auth_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: YOUR_DB_USERNAME        # e.g. root
    password: YOUR_DB_PASSWORD        # e.g. Pass@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost
    ip-address: 127.0.0.1

# JWT Secret - Must match the same secret used in api-gateway and other services
jwt:
  secret: YOUR_BASE64_JWT_SECRET_KEY

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
